load("@npm//webpack:index.bzl", "webpack")

webpack(
    name = "build_host",
    args = [
        "--env entry=./$(execpath host.jsx)",
        "--output-path=$(RULEDIR)",
        "--config=bazel/js/webpack.host.config.js"
    ],
    data = [
        "@npm//:node_modules",
        "host.jsx",
        "//bazel/js:webpack.host.config.js",
        "//:package.json"
    ],
    output_dir = True
)