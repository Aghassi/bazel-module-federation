load("@npm//webpack:index.bzl", "webpack")

webpack(
    name = "build",
    chdir = package_name(),
    args = [
        "--output-path=$(RULEDIR)"
    ],
    data = [
        "@npm//:node_modules",
        "host.jsx",
        "webpack.config.js",
        "//:package.json"
    ],
    output_dir = True
)