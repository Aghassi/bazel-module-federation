load("//bazel/js:defaults.bzl", "build_host")

build_host(
    srcs = [
        "DynamicComponent.tsx",
        "FederatedRoute.tsx",
        "bootstrap.tsx",
        "capabilities/preload.ts",
        "host.tsx",
    ],
    data = [
        "//src/utils",
    ],
    entry = "host",
)
