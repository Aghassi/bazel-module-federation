load("//bazel/js:defaults.bzl", "build_host", "pkg_web")

build_host(
    data = [
        "DynamicComponent.tsx",
        "Route.tsx",
        "bootstrap.tsx",
        "capabilities/getIdentifier.ts",
        "capabilities/preload.ts",
        "host.tsx",
    ],
    entry = "host",
)

pkg_web(
    name = "host",
    srcs = [
        ":host_build",
    ],
    additional_root_paths = ["%s/host_build" % package_name()],
    visibility = ["//src/client:__pkg__"],
)
