load("//bazel/js:defaults.bzl", "build_module", "build_road", "generate_route_manifest", "pkg_web")

build_road(
    name = "default",
    data = ["default.tsx"],
    entry = "./$(execpath default.tsx)",
)

pkg_web(
    name = "routes",
    srcs = [
        ":default",
        "//src/client/routes/secondary",
    ],
    additional_root_paths = ["%s/secondary" % package_name()],
    visibility = ["//src/client:__pkg__"],
)

generate_route_manifest(
    name = "route_manifest",
    data = ["routes"],
)

build_module(
    package_name = "@carto/routes",
    srcs = [
        "index.ts",
        ":route_manifest",
    ],
    visibility = ["//:server_group"],
    deps = [
        "@npm//@types/node",
    ],
)
