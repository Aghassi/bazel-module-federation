load("//bazel/js:defaults.bzl", "build_module")

build_module(
    package_name = "@carto/utils",
    srcs = [
        "index.ts",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "@npm//@types/node",
    ],
)
